nerdfont: true

merge_strategy: rebase
agent: claude

files:
  copy:
    - .env
    - .env.prod
    - certs/
    - infra/terraform.tfvars

panes:
  - command: <agent>
    focus: true
  - command: pnpm install
    split: vertical
