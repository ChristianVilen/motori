nerdfont: true

merge_strategy: rebase
agent: claude

files:
  copy:
    - .env
    - .env.prod
    - infra/certs/
    - infra/secrets/
    - infra/terraform.tfvars
    - infra/terraform.tfstate

panes:
  - command: <agent>
    focus: true
  - command: pnpm install
    split: vertical
