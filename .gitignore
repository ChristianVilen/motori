node_modules
**/e2e/.auth/
**/e2e/.test-results/
# Playwright default output dir — created when playwright runs outside apps/*
test-results/
playwright-report/
.output
dist
.env
.env.prod
.worktree-offset
*.local
.vinxi
.vite
.tanstack
.superpowers/
.claude/worktrees/
.claude/settings.local.json
.pi
.idea

# Raw image originals — optimized output goes to public/images/
/images/
!/images/.gitkeep

# Uploaded images (local dev)
/uploads/
/apps/motori/uploads/
*:Zone.Identifier

# Encrypted secrets backup (age-encrypted dokku config:export)
secrets/**
!secrets/.gitkeep
!secrets/**/
!secrets/**/*.age
.DS_store

# Private document uploads (local dev) — served only via the authenticated proxy
uploads-docs/
.playwright-cli/
