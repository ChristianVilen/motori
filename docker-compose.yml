name: motori

services:
  db:
    image: postgres:17-alpine

    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: motori
      POSTGRES_PASSWORD: motori
      POSTGRES_DB: motori
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
